# HANDOFF

最終更新: 2026-09-04（セッション: 沖縄県知事選を**別の7つの角度**から掘って
7本を作り、9/5〜9/11 に1日1本ずつ予約中。説明文で候補者が重複する既存の欠陥を
修正。HANDOFF が指していたのに存在しなかった `check_telop.py` を実装）

## いま何をしているのか

**2026年沖縄県知事選挙（告示 8/27・投開票 9/13）の動画を、前回の5本に加えて
7本作り、9/5〜9/11 の枠に1本ずつ予約している。** 一次資料は前回と同じく
`scripts/election.py` の許可リスト（候補者本人の公約ページ・政策集PDF）で、
**採用ゲートも許可リストも一切広げていない。**

前回の5本は「公約全体の要約」と「家計に直結する項目」に寄っていたので、
今回は**分野を7つに割って**掘った（基地×2・交通・経済・離島・医療・エネルギー）。

`ANTHROPIC_API_KEY` は無いままなので、台本は対話セッションが書いて
`--script` で渡す運用も変わっていない。

## ⚠️ この7本のアップロードは**まだ走っている途中**

バックグラウンドで `scripts/run_election.py` を7本ぶん順に実行している。
**最終更新の時点で完了しているのは1本目だけ。** 残り6本の予約が通ったかは
**未検証**。再開したらまず下の「次にやること」1番を実行して現況を確かめること。

| 公開予定（JST） | 台本 | 候補 | 題材 | 状態 |
|---|---|---|---|---|
| 09-05 07:30 | `tamaki_peace.json` | 玉城 | 平和35項目・辺野古反対・地位協定 | **予約済 `nhEm9sGFMuw`** |
| 09-06 07:30 | `koja_peace.json` | 古謝 | 基地9項目・辺野古容認・南西シフト | 実行中／未確認 |
| 09-07 18:30 | `koja_traffic.json` | 古謝 | インフラ13項目「沖縄移動革命」鉄軌道 | 未確認 |
| 09-08 18:30 | `tamaki_economy.json` | 玉城 | 経済57項目「ザル経済からの脱却」 | 未確認 |
| 09-09 18:30 | `tamaki_islands.json` | 玉城 | 離島25項目・通院交通費・遠隔医療 | 未確認 |
| 09-10 18:30 | `koja_health.json` | 古謝 | 健康長寿11項目・北部基幹病院 | 未確認 |
| 09-11 18:30 | `tamaki_energy.json` | 玉城 | エネルギー16項目・2050年脱炭素 | 未確認 |

枠の取り方: 9/5・9/6 の 18:30 は前回の5本で埋まっているので、この2日は
**07:30 の枠**を使った。9/7 以降は 18:30。**9/12・9/13 は使っていない**
（9/13 は投票日当日で、選挙関連の投稿・更新が公職選挙法129条で禁止）。

### 前回セッションで予約済みの5本（そのまま）

| 公開予定 | video id | 題材 |
|---|---|---|
| 09-02 18:30 | s0H-HHUiUfI | 古謝氏の公約（三つの倍増・辺野古容認） |
| 09-03 18:30 | VqhHb3TfAgg | 玉城氏の政策（7つの柱・鉄軌道） |
| 09-04 18:30 | Ut919N6NctI | 古謝氏の「三つの倍増」を数字で確かめる |
| 09-05 18:30 | TXa3-6tCvkk | 古謝氏の政策集120項目を読む |
| 09-06 18:30 | L2hB5naZk5o | 玉城氏の政策264項目を読む |

合わせて12本。候補ごとの本数は**古謝6本・玉城6本**で揃えてある。

## 今回やったこと

### 1. 立候補者は6人だと分かった（前回の前提が違っていた）

前回の引き継ぎは「主要2候補」としか書いていなかったが、**8/27 の告示で
届け出たのは6人**（過去最多）。古謝玄太・兼島俊・末吉正広・玉城デニー・
比嘉隆・屋良朝助の各氏で、報道は古謝・玉城の**事実上の一騎打ち**としている。

**残り4氏は、公約の全文を公式サイトで公表しているものが見つからなかった。**
（web検索で確認。報道機関のまとめ記事はあるが、報道は一次資料に使えない。）
→ `election.MANIFESTO_SOURCES` は2氏のままにした。**許可リストは広げていない。**

### 2. `scripts/run_election.py` — 説明文の欠陥を2つ修正

実際に出来上がった `description.txt` を読んで見つけた既存の欠陥:

- **同じ候補が2回並んでいた。** `MANIFESTO_SOURCES.values()` をそのまま
  並べていたが、この辞書は1人につき複数（要約ページ＋政策集PDF）を持つ。
  実物（`work/election-koja-detail-09051830/description.txt`）では
  古謝2行・玉城2行の**計4行**になっていた。→ 候補ごと1行にまとめた。
- **「各候補の公約（公式サイト）:」の下に2人ぶんしか無く、動画単体では
  「候補は2人」と読めた。** 実際は6人。→ `CANDIDATE_COUNT = 6` を置き、
  「この選挙には6人が立候補しています。この動画で扱うのは、公約の全文を
  公式サイトで公表している次の2氏です。」と書くようにした。

`tests/test_election.py` に2件追加（1人1行であること／届け出者数を書くこと）。

### 3. `scripts/check_telop.py`（新規）— 引き継ぎが指していたのに無かった

前回の HANDOFF は「台本を書いたら `check_telop.py` で幅を測ってからビルドする」
と書いていたが、**リポジトリにこのファイルは存在しなかった**（前回セッションの
使い捨てスクリプトだったと思われる）。実装して常設にした。

`render_telop` と**同じ割り付け・同じフォント縮小手順**で幅と行数を測る。
音声は要らない（`telop.spans` が音に合わせるのは表示時刻だけで、どの文字が
ひとまとまりになるかは本文だけで決まるため）。

**はみ出しの判定を `AVAIL` で行わないこと。** `draw.wrap` は禁則処理のため、
行頭に置けない文字（「、」「。」）を前の行に残して `AVAIL` を意図的に超える。
そこで数えると正常な出力を毎回落とす。判定は**画面から出るか**（左マージン＋
描画幅＋黒フチ > 1080px）で行っている。

## 検証済みの事実（実際に画面に出した出力）

- **`pytest` 587 passed**（`test_election.py` に2件追加後）。
- **7本の台本は全件、一次資料に逐語で存在することを機械的に確認した。**
  `election.collect()` を実際にページを取得して7本ぶん通した（全件 OK）。
  あわせて字数・各項目の上限・`quote_excerpt` が引用の部分文字列であること・
  `ground_excerpt` が差し替えを要求しないことも同時に確認している。
  本文は 334〜351字（許容 330〜355）。
- **テロップのはみ出しは7本とも0**（`check_telop.py`）。最大描画幅は
  924〜990px で、切れるのは 1011px 超。禁則で右余白に食い込む行は
  **予約済みの5本にも同じだけある**（最大 1000px）ので、今回入った現象ではない。
- **1本目の尺は 58.67秒**（`ffprobe`）。合成は `speedScale=0.994` で 58.63秒。
- **1本目の説明文が修正後の形になっていることを実物で確認した**
  （候補は1人1行・「6人が立候補」の1文が入っている）。
- **1本目の完成画（`stage.png`）を目視で確認した。** 見出し「辺野古新基地建設に
  反対」・玉城氏の写真・テロップ・引用カード（出典キャプション付き）が正常。
- **立候補者6人**（web検索で確認。琉球新報・八重山日報の告示日の記事）。
- 画像は前回と同じ ja.wikipedia の記事画像を**コピーして使い回した**
  （古謝＝`Genta_koja_(3x4).jpg` CC BY-SA 4.0・`frame_photo.py` 適用済み /
  玉城＝`Denny_Tamaki.jpg` CC BY 4.0）。`run_election` は `photo.jpg` と
  `license.json` が既にあれば取得を飛ばすので、同じ素材が確実に再現される。

## 未検証のもの

- **7本のうち6本の予約が通ったかは未確認**（上の表）。
- **12本とも実際に公開されるかは未確認。**
- **予約済みの5本の説明文は古いまま**（候補が重複し、6人と書いていない）。
  今回直したのは**これから作る動画のテンプレート**だけ。既存動画の説明文を
  書き換える経路は `upload_youtube.py` に無い（`--publish` / `--schedule` /
  `--publish-id` のみ）。直すなら小さな更新スクリプトが要る。**未対応。**
- **残り4候補は扱えていない。** 公約全文の公式ページが見つからなかったため。
  これは「2氏だけを扱う」という編集判断ではなく**資料の有無**によるもので、
  説明文にその理由を書いてある。
- **選挙運動にあたるかの判断は未整理**（前回から変わらず）。12本とも
  「候補者が公表している内容をそのまま紹介する」形で、賛否は書いていない。
  公職選挙法142条の3第3項の連絡先表示については**法的助言ではない**。
- **TikTok 版は作っていない。** `run_election.py` は TikTok を触らない。
- TikTok の審査状況は2セッション前から未確認。

## 次にやること

1. **まず7本の予約状況を引き直す。** これが最優先:

   ```bash
   python -c "import json;d=json.load(open('state/published.json',encoding='utf-8'))['videos'];[print(v['publish_at'],k,v['youtube_video_id']) for k,v in sorted(d.items(),key=lambda x:x[1].get('publish_at') or '') if (v.get('publish_at') or '')>='2026-09-05']"
   ```

   7件そろっていなければ、足りないものを個別に流し直す（枠と id は上の表）:

   ```bash
   python scripts/run_election.py --candidate koja-detail --script work/scripts/koja_peace.json --id election-koja-peace-09060730 --slot 2026-09-06T07:30:00+09:00
   ```

   **さらに YouTube API で `status.publishAt` を引き直して確かめる。**
   アップロード直後の `videos.list` は `publishAt` が欠けて返ることがあるので、
   欠けていても即座に作り直しに走らないこと（下の「触ってはいけないところ」）。
2. **今日 9/4 18:30 に `Ut919N6NctI` が公開される。** 事故があれば
   `python scripts/unpublish.py Ut919N6NctI` で即座に戻せる。
3. **予約済み5本の説明文を直すか決める**（上記「未検証のもの」）。
   直すなら `videos().update(part="snippet")` を使う小さなスクリプトを足す。
4. **9/12 の枠は空けてある。** 使うなら選挙以外の題材にするか、
   選挙ものなら 9/12 中に公開が終わる時刻にすること。
   **9/13（投票日当日）は選挙関連の投稿・更新をしない。**
5. **選挙が終わったら（9/13以降）`scripts/election.py` と
   `scripts/run_election.py` を消す。** テスト `tests/test_election.py` も。
   消せば元の1経路に戻る。`check_telop.py` は選挙と無関係なので**残す。**
6. **`com.-youtube` の Google OAuth トークンを失効・再発行する**（前回から
   持ち越し）。履歴に残ったトークンのせいで Push Protection に拒否され、
   あのリポジトリは push できないままになっている。**迂回はしていない。**
7. 維持率の確認（`python scripts/retention_report.py`）。選挙12本は通常の
   題材と傾向が違うはずなので、平常の題材選びの判断材料に混ぜないこと。

## 触ってはいけないところ

- **`evidence.EVIDENCE_HOST_SUFFIX` を広げない。** 今回も触っていない。
- **`election.MANIFESTO_SOURCES` を候補ごとに不揃いにしない。**
  候補ごとの件数が揃っていることをテストが縛っている。
  **報道機関のまとめ記事を一次資料に足さない**（残り4候補を扱いたくなるが、
  逐語照合の相手が一次資料でなくなる）。
- **`election.MIN_PDF_CHARS` / `MIN_KANA_RATIO` を下げない。**
  玉城氏の政策集PDFは全文が文字化けする（ひらがな比率 0.000）ので
  `SourceUnreadable` で弾かれる。**詳細は HTML の政策ページを使う**（今回もそう）。
- **`election.collect` の逐語照合を緩めない。** これがこの経路唯一の関門。
- **`check_telop.py` の判定を `AVAIL` に戻さない。** 禁則処理で正常に
  `AVAIL` を超えるため、正常な出力を毎回落とすことになる（上記）。
- **`build_short._fill` を「切り取らない」方式に変えない。** 顔が切れるのは
  素材側の問題なので `frame_photo.py` で直す。
- **`draw._BACKTRACK_RATIO` を下げすぎない。** 0.6 は実測158か所で決めた値。
- 台本ファイルの `source_url` / `source_quote` 必須をやめない。
- **台本を書いたら `check_telop.py` で幅を測ってからビルドする**（今度は実在する）。
- **`state/*.json` を手で編集しない。**
- チャンネルを動かす操作の前に main を取り込んで state を最新にする。
- **アップロード直後の `videos.list` は `tags` や `status.publishAt` が欠けて
  返ることがある。** 予約が外れたと判断する前に必ず引き直すか YouTube Studio を
  見る。ここで `unpublish.py` → 作り直しに走ると無事な動画を1本捨てる。
- **9/13（投票日当日）に選挙関連の投稿・更新をしない**（公職選挙法129条）。
