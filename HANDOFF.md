# HANDOFF

最終更新: 2026-09-07（セッション: **アナリティクスの調査と、次の題材の洗い出し**。
投稿・制作はしていない。`docs/analytics-2026-09-07.md` と
`docs/topic-candidates-2026-09-07.md` にまとめた。動画別レポートから1日分が
欠けて返る罠を踏んだので `docs/known-issues.md` 15番に記録した）

## いま何をしているのか

**制作は止まっていない。** 沖縄県知事選（投開票 9/13）の動画が 9/11 まで
18:30 枠に予約済みで、9/2〜9/6 の5本は公開されて再生も付いている。

今回のセッションは**数字を見ただけ**。コードもパイプラインも触っていない。
追加したのは調査レポート1本と known-issues 1件だけ。

`ANTHROPIC_API_KEY` は無いままなので、台本は対話セッションが書いて
`--script` で渡す運用も変わっていない。

## 公開済み・予約済みの選挙動画（2026-09-07 に API で引き直して確認済み）

| 枠（JST） | video id | 状態 | 再生 |
|---|---|---|---|
| 09-02 18:30 | s0H-HHUiUfI | public | 2,527 |
| 09-03 18:30 | VqhHb3TfAgg | public | 1,542 |
| 09-04 18:30 | Ut919N6NctI | public | 1,147 |
| 09-05 18:30 | TXa3-6tCvkk | public | 1,401 |
| 09-06 18:30 | L2hB5naZk5o | public | 1,323 |
| 09-07 18:30 | RhOoYz4TrpE | private・予約済み | — |
| 09-08 18:30 | OfJk7NWKXTU | private・予約済み | — |
| 09-09 18:30 | mxfqaTDzAYw | private・予約済み | — |
| 09-10 18:30 | Md3w6Qaqx5Y | private・予約済み | — |
| 09-11 18:30 | R3SZbpIoI5U | private・予約済み | — |

**07:30 の枠は 9/3 以降ずっと空**（1日2本 → 1本になっている）。

予約を外した旧版が8本、非公開のままチャンネルに残っている（`publishAt=None` 確認済み）。
不要なら YouTube Studio から手で消すこと:
`vvpSRwF072M yHT9_jXuvlY BgIhdx43dSo -K_GQlJ4oR0 wL43dLccqLg phur61vle8A LOYwJw6tBUo BKwnK8HFczE`

## 今回やったこと

**`docs/analytics-2026-09-07.md`（新規）** — 2026-08-12〜09-07 の実測。
**`docs/known-issues.md` 15番（新規）** — 動画別レポートの欠け。
**`docs/topic-candidates-2026-09-07.md`（新規）** — 候補の検索語32語を
採用ゲートに実際に当て、引用を読んで選び直した題材の候補。

## 検証済みの事実（実際に画面に出した出力）

すべて YouTube Analytics API v2 / Data API v3 から取得。投稿系は一切叩いていない。

- **27日間で 93,001再生・705.5時間・登録純増 +63**（現在 2,910人）。
- **流入の 94.9% が Shorts フィード**、検索 3.0%、**関連動画は2再生**。
  モバイル 82.3%、日本 99.6%。**視聴者の 74.5% が55歳以上、85.4% が男性**。
- **1本あたりの再生は 8/22 あたりで床に着いて横ばい**。公開週ごとの中央値は
  第33週 2,993 → 第34週 1,465 → 第35週 1,277 → 第36週 1,542。
  **尺は43本すべて59秒**なので、平均視聴秒の低下（55s → 39s）は尺のせいではない。
- **公開後3日で決着する**。7日以上たった40本で「初3日 ÷ 累計」の中央値は **99%**。
- **決めているのは冒頭**。12秒地点の残存率で3等分すると、下位69%→1,236再生 /
  中位92%→1,324 / 上位110%→1,862。3秒地点だけでも再生数との相関は r=0.67 で
  維持率全体と同じ。**落ちる場所は全部が冒頭**で、50%地点以降の傾きは勝ち負けで
  ほとんど変わらない。
- **Shorts フィードにほぼ配信されなかった動画が2本ある**:
  `R_dirwcTjqs`（27再生）と `8639Yg7Ner0`（7再生）。他41本は全部 997再生以上。
  公開状態・尺59秒・1080x1920・30fps・エンコード・`uploadStatus` は他と同一で、
  **制作側の欠陥は見つからなかった。原因は不明。**
- **長尺 `UqjB--sNTKk` は累計5再生**（8/20公開）。流入は `SHORTS_CONTENT_LINKS` 5 のみ。
- **`published.json` の `2D_cpARVcw0` はチャンネルに存在しない**（`videos.list` で引けない）。
- **`recipes/` 56件すべて `evidence.figure` が空**。これは欠陥ではなく、
  e-Stat 系統を外した結果（`evidence.py` の注記どおり）。
- **動画別レポートから 08-31 の1日分が欠けて返った**（known-issues 15番）。
  日別合計 93,001 は動かず、動画別合計だけが 93,013 → 89,415 に落ちた。
- **候補の検索語 32語のうち 31語が採用ゲートを通った**（落ちたのは `NHK 受信料` だけ）。
  **ゲートは題材の選別に使えない。** 実例: `携帯電話 料金` は13件通るが、
  1位の引用は**航空券の転売の話**だった。
- **引き継ぎに「未使用の候補」として残っていた2語は、引用を読むと弱い。**
  `医師 偏在` は18件通るが上位の引用が**医薬品の供給偏在**の話、
  `外国人 土地` は16件通るが骨太方針への言及止まりで数字が立たない。
- **発言者に ja.wikipedia の記事画像がある動画のほうが数字が良い**:
  写真あり n=24 は再生中央値 1,560・12秒残存 100%、
  代替画像（国会議事堂）n=19 は 1,263・84%。**ただし題材の大きさと交絡している。**

## 未検証のもの

- **配信されなかった2本の原因は分からない。** 制作物側に差が無いことまでしか
  確かめていない。YouTube 側の判断か一時的な事故かは切り分けられていない。
- **題材の効き方は n が小さすぎる。** 税 n=4（中央値2,991）、対外 n=2（3,164）は
  方向は強いが、この本数で配点（`sources.PLUS`/`MINUS`）を動かす根拠にはならない。
- **「冒頭12秒で決まる」の因果は未確認。** 相関しか測っていない。冒頭の書き方を
  変えて再生数が動くかは試していない。
- 9/7〜9/11 の5本が実際に公開されるかは未確認（予約が入っていることだけ確認）。
- TikTok の審査状況は未確認。

## 次にやること

1. **`retention_report.py` に日別合計との突き合わせを入れる。**
   いまは欠けた数字がエラー無しで出てくる（known-issues 15番）。
   日別合計と動画別合計を両方表示して、ずれていたら警告するだけでよい。
   **これを入れるまで、このCLIの出力を判断に使わないこと。**
2. **冒頭の書き方を変えて測る。** 冒頭3秒の残存率が結果をほぼ決めているので、
   ここだけを変えた比較が一番効く。**1回に1つだけ変える。**
3. **07:30 の枠を埋める。** 9/3 以降 空いている。題材の候補と引用・出典URLは
   `docs/topic-candidates-2026-09-07.md` に8件そろえてある。上から
   `復興特別所得税 防衛` / `相続税 基礎控除` / `ガソリン 補助金`。

   ```bash
   python scripts/run_daily.py --keyword "復興特別所得税 防衛" --script <台本.json> --days-ahead 1 --limit 1
   ```
4. **`published.json` から `2D_cpARVcw0` のエントリを外す**（known-issues 8番の
   例外にあたる唯一のケース）。**それ以外の手編集はしない。**
5. **選挙が終わったら（9/13以降）`scripts/election.py` と `scripts/run_election.py`、
   `tests/test_election.py` を消す。** 消せば元の1経路に戻る。
6. `com.-youtube` の Google OAuth トークンを失効・再発行する（前回からの持ち越し。
   履歴にトークンが残っていて Push Protection に止められている。**迂回はしていない**）。

## 触ってはいけないところ

- **数字を根拠に採用ゲートを緩めない。** 「伸びる題材」が一次資料を取れないなら
  作らない。設計上、成果の最適化が触ってよいのは
  `sources.PLUS`/`MINUS` と `keywords.POLICY` までで、関門は対象外。
- **今回の題材別の数字（税 n=4 など）で配点を動かさない。** 本数が足りない。
- **「採用ゲートを通ったか」で題材を選ばない。** 32語中31語が通る。
  引用を読んで、見出しを裏付けているかを人が見る。
- **`retention_report.py` の出力を、日別合計と突き合わせずに比較へ使わない**（15番）。
- **`evidence.EVIDENCE_HOST_SUFFIX` を広げない。**
- **`election.MANIFESTO_SOURCES` を候補ごとに不揃いにしない。**
- **`election.MIN_PDF_CHARS` / `MIN_KANA_RATIO` を下げない。**
- **`build_short._fill` を「切り取らない」方式に変えない。** 顔が切れるのは素材側の
  問題なので `frame_photo.py` で直す。
- **`draw._BACKTRACK_RATIO` を下げすぎない。** 0.6 は実測158か所で決めた値。
- 台本ファイルの `source_url` / `source_quote` 必須をやめない。
- **台本を書いたら `check_telop.py` で幅を測ってからビルドする。**
- `state/*.json` を手で編集しない（上記4番の1件を除く）。
- チャンネルを動かす操作の前に main を取り込んで state を最新にする。
- **9/13（投票日当日）に選挙関連の投稿・更新をしない**（公職選挙法129条）。
