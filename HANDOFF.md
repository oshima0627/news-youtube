# HANDOFF

最終更新: 2026-09-02（セッション: 沖縄県知事選の**5本**を作って予約。選挙用の別経路
`run_election.py` を新設。公約PDFにも対応。Wikimedia のサムネイルホストの
取りこぼし、タイトな顔写真が切れる問題、テロップが語の途中で改行される
問題を修正。**同じ改行の修正を他リポジトリへ展開**）

## いま何をしているのか

**2026年沖縄県知事選挙（告示 8/27・投開票 9/13）の動画を5本、9/2〜9/6 の
18:30 枠に予約した。** 一次資料は**候補者本人の公式公約ページ**で、
これは `run_daily.py` の採用ゲート（`.go.jp` 限定）を通らないため、
**別経路 `scripts/run_election.py` を新設**した。日次パイプラインの関門は
一切触っていない。

`ANTHROPIC_API_KEY` は無いままなので、台本は対話セッションが書いて
`--script` で渡す運用も変わっていない。

## 予約した3本（2026-09-02 16:00 JST 時点・API で引き直して確認済み）

| 公開予定（JST） | video id | 題材 |
|---|---|---|
| 09-02 18:30 | s0H-HHUiUfI | 古謝氏の公約（三つの倍増・辺野古容認） |
| 09-03 18:30 | VqhHb3TfAgg | 玉城氏の政策（7つの柱・鉄軌道） |
| 09-04 18:30 | Ut919N6NctI | 古謝氏の「三つの倍増」を数字で確かめる |
| 09-05 18:30 | TXa3-6tCvkk | 古謝氏の政策集120項目を読む（深掘り） |
| 09-06 18:30 | L2hB5naZk5o | 玉城氏の政策264項目を読む（深掘り） |

### 予約を解除して残してある旧版（削除していない）

写真の切れとテロップの改行を直すたびに作り直したため、**非公開・予約なし**の
動画が8本チャンネルに残っている。`publishAt=None` を確認済みなので放置しても
公開されないが、不要なら YouTube Studio から手で消すこと。

```
写真の切れで差し替え: vvpSRwF072M  yHT9_jXuvlY  BgIhdx43dSo
改行の修正で差し替え: -K_GQlJ4oR0  wL43dLccqLg  phur61vle8A  LOYwJw6tBUo  BKwnK8HFczE
```

`state/published.json` は常に最新の ID を指している。

## 今回やったこと

### 1. `scripts/election.py`（新規）— 公約ページ用の採用ゲート

- `MANIFESTO_SOURCES` に**両候補の公約ページ2件だけ**を URL ごとハードコード。
  ホスト単位で許可しない（そのドメインの任意のページが通ってしまうため）。
- `collect(candidate, quote)` は、**引用がページに逐語で存在するか**を機械的に
  確認する。無ければ `QuoteNotFound` で止まる。空白のゆれだけは吸収する。
- `figure` は `evidence.has_figure` で検証（判定基準を2箇所に書かない）。
- **PDF の政策集にも対応**（`_pdf_text`）。テキスト層が 500字未満なら
  `SourceUnreadable` で止める。画像だけのPDFを通すと、逐語照合が
  「一致しない」ではなく「照合対象が無い」状態になり関門が無効になるため。
- 許可リストは**候補ごとに同数**であることをテストで縛っている
  （要約ページ2件＋政策集PDF2件＝計4件）。片方だけ詳しい資料が使える状態だと、
  深掘りが常に一方に偏る。
- テスト `tests/test_election.py` 13件。

### 2. `scripts/run_election.py`（新規）— 選挙用の実行入口

台本より後ろ（引用カードの検証・音声合成・動画合成・アップロード）は
`run_daily.py` と**同じ関数**を呼ぶ。違うのは一次資料の出どころだけ。
画面のカードは常に引用カード（`Evidence.figure` を空で作る）。
説明文には**両候補の公式サイトを併記**する。

```bash
python scripts/run_election.py --candidate koja \
    --script work/scripts/koja_manifest.json \
    --slot 2026-09-02T18:30:00+09:00 [--dry-run]
```

### 3. `scripts/frame_photo.py`（新規）— 顔が切れる写真を帯の比率に整える

`build_short._fill` は帯（1080x740）を覆うように拡大して切り取る。**もともと
タイトに切り抜かれた顔写真**では頭頂とあごが窓の外に出る。古謝玄太の
ja.wikipedia 記事画像（550x733・余白なし）で実際に起きた（1439px に拡大して
740px だけ切り出すため 699px を捨てる）。

`_fill` 側を変えると、余白のある素材（玉城デニーの 1280x1707。切り取っても
自然に収まる）の構図まで一緒に変わる。**素材が原因の問題は素材側で直す。**
帯と同じ縦横比になるまで左右にぼかし背景を足し、`_fill` に切り取る余地の
無い画像を渡す。元画像は `photo_source.jpg` に残すので再実行しても同じ結果。

```bash
python scripts/frame_photo.py work/<id>   # そのあと build し直す
```

テスト `tests/test_frame_photo.py` 4件。

### 4. `scripts/draw.py` — テロップが語の途中で改行される問題（既存欠陥の修正）

`wrap()` は幅だけを見て折り返しており、**実測158か所のうち36%が語の途中**で
割れていた（「してい」／「る」、「辺野古移」／「設」、「231万」／「5000円」、
見出しの「県民所得を500万」／「円に」）。数字が行をまたぐと別の額に読める。

直した点は2つ:

1. **数値は単位までひとかたまり**にする（`_ATOM_RE`）。
   「1兆747億円」「231万5000円」「67パーセント」が割れない。
2. **幅で決まった位置が語の途中なら、自然な切れ目まで戻して切る**
   （`_choose_break` / `_break_score`）。優先度は
   句読点の直後 > かっこの直前 > 助詞の直後 > ひらがな→漢字の変わり目。
   戻しすぎて短い行ができないよう `_BACKTRACK_RATIO = 0.6` で下限を切る。

**不自然な改行は 57/158（36%）→ 8/158（5%）。** はみ出しは0のまま。
テスト `tests/test_draw.py` に6件追加。

### 5. 同じ改行の修正を他リポジトリへ展開した

`projects` 配下 41 リポジトリを調べ、**自動で改行位置を決めているのは5つ**。
同じ5文で測った結果（`bgm-youtube` はサムネの2行割りなので別基準）:

| リポジトリ | 変更前（不自然/総数, 数値の分断） | 変更後 |
|---|---|---|
| news-youtube | 10/12, 2 | 1/15, 0 |
| tora-kirinuki | 11/13, 2 | 1/15, 0 |
| hiroyuki-youtube | 7/14, 1 | 2/15, 0 |
| com.-youtube | 2/14, 1 | 2/15, 0 |
| bgm-youtube | サムネ2行割り。数値・カタカナを分断 | 分断 0 |

**対象外と確認したもの**（実際に中を見た）:
`NexeedLab/gen_header.py` `gen_carousel.py` は `text.split("
")` で
**改行を手書きで持っている**。`hiroyuki/thumbnail.py` の `main` も
レシピJSONに手書きの配列。`amazon-affiliate` `com.-youtube/thumbnail.py`
`tora-kirinuki/cards.py` `news-youtube/cards_wide.py` は自前で折らず、
修正済みの `wrap()` を呼ぶだけ。

- **tora-kirinuki**: `wrap()` が幅を見て1文字ずつ折るだけだったので、
  news-youtube と同じ実装（atom・切れ目の採点・括弧の深さ）を入れた。
  `tests/test_draw.py` 6件を新設。**pytest 200 passed。push 済み。**
- **hiroyuki-youtube**: 読点・括弧の処理は既にあった（2026-08-14）。
  数値＋単位の保護と、助詞・ひらがな→漢字の優先を足した。既存の
  「括弧の中で折らない」挙動は旧実装と出力を比較して不変を確認。
  テストが無かったので `tests/test_draw.py` 4件を新設。**push 済み。**
- **com.-youtube**: 文字種変化の採点まで持っていたが、「万」と「5」も
  文字種変化にあたるため**数値を優先して割っていた**。数値の内側を
  0点にした。`pytest 143 passed`。**push できていない（下記）。**
- **bgm-youtube**: `build_video.wrap_lines()` は左右の幅が揃う位置を選ぶだけで、
  「作業用BGM1」／「000万回再生」「深夜の3時」／「間ジャズ」「昼のス」／
  「ウィング」と割れていた。数値＋単位とカタカナ語を候補から外した
  （英語を空白でしか切らないのと同じ理由）。`pytest 212 passed`。**push 済み。**
- **news-youtube**: 他リポジトリと突き合わせた結果、hiroyuki が既に
  解いていた「括弧の中では折らない」が無いと分かったので追加した。
  **予約済み5本のテロップ出力は変化なし**（比較して確認）。

#### com.-youtube は push できていない

`git push` が GitHub の **Push Protection（シークレット検出）** で拒否される。
原因は**私の変更ではなく、以前からローカルに溜まっていた自動コミット**:

```
45bc225 chore: 作業終了時の自動コミット（2026-09-02 16:45）
  → token.json.expired-20260814 に Google OAuth の access / refresh token
```

**迂回はしていない。** ローカルには4コミット（自動コミット2件＋今回の修正＋
マージ）が積まれたまま。対応は本人の判断が要る:

1. **まず該当の Google OAuth トークンを失効・再発行する**（履歴に残っており、
   push 直前まで行っている）
2. そのうえで履歴からファイルを落とす（`git filter-repo` 等）か、
   GitHub の unblock URL で許可する
3. `.gitignore` に `token.json*` を足す（現在は `token.json` のみ）

### 6. `scripts/photos.py` — Wikimedia のサムネイルホストを許可（既存欠陥の修正）

commons の imageinfo が `thumb.wikimedia.org` を返すことがあり、許可リストが
`upload.wikimedia.org` だけだったため `download()` が `ValueError` を投げていた。
`resolve` 側の汎用画像フォールバックはこの失敗を拾わないので、
**run_daily ではその題材が丸ごと見送られる**。玉城デニーの記事画像で実際に踏んだ。
`WIKIMEDIA_HOSTS` に両方を入れ、`attribution()` の分岐も両方に広げた。
テスト3件追加。

## 検証済みの事実（実際に画面に出した出力）

- **`pytest` 585 passed**（`election` 15件・`photos` 3件・`frame_photo` 4件・
  `draw` 6件の追加後）。
- **5本とも予約が通った。API で引き直して確認した**:

  ```
  s0H-HHUiUfI PT59S private 2026-09-02T09:30:00Z （= 9/2 18:30 JST）
  VqhHb3TfAgg PT59S private 2026-09-03T09:30:00Z （= 9/3 18:30 JST）
  Ut919N6NctI PT59S private 2026-09-04T09:30:00Z （= 9/4 18:30 JST）
  TXa3-6tCvkk PT59S private 2026-09-05T09:30:00Z （= 9/5 18:30 JST）
  L2hB5naZk5o PT59S private 2026-09-06T09:30:00Z （= 9/6 18:30 JST）
  ```

  旧版8本は `publishAt=None`（予約解除済み）を確認した。
- **改行の改善を数えて確認した**: 5本のナレーション158か所の改行のうち、
  語の途中で割れているものが 57 → 8 に減った。
- **尺は5本とも 58.34〜58.63秒**（voice.wav と video.mp4 の差 +0.03秒以内）。
- **テロップのはみ出しは5本とも0**（`check_telop.py` で全行の描画幅を測定）。
  字数は 349 / 341 / 344 / 343 / 352。
- **古謝氏の政策集PDFはテキスト層あり**（14ページ・9078字）。
  **玉城氏の政策集PDFは全文が文字化けする**（ひらがな比率 0.000。埋め込み
  フォントに ToUnicode マッピングが無い）。`_assert_readable` が
  `SourceUnreadable` で弾くので**この資料は使えない**。玉城氏の詳細は
  HTMLの政策ページ（15074字・7つの柱の全項目）に載っているのでそちらを使った。
- **項目数を数えて確認した**: 古謝＝10分野120項目。
  玉城＝7つの柱で 57+87+20+25+24+16+35 = 264項目。
- **120項目の内訳を数えて確認した**: 暮らし9・経済21・地域振興11・インフラ13・
  子育て17・人材育成10・健康長寿11・安心安全11・平和と基地負担9・つなぐ8 = 120。
- **引用は4本とも一次資料に逐語で存在することを機械的に確認した**
  （`election.collect` が通った）。さらに `ground_excerpt` が
  `quote_excerpt` を引用の部分文字列として検証している。
- **画像は両候補とも ja.wikipedia の記事画像**（規約どおりの唯一の経路）:
  古謝＝`Genta_koja_(3x4).jpg` CC BY-SA 4.0 / 玉城＝`Denny_Tamaki.jpg` CC BY 4.0。
- **選挙日程**: 告示 2026-08-27、投開票 2026-09-13（web 検索で確認）。
- **国会会議録には `古謝 玄太` が 0件**（`evidence.collect` の実測）。
  `玉城 デニー` は20件あるが**すべて他の議員が彼について語った発言**で、
  本人の公約ではない。→ 既存パイプラインでは今回の3本は作れない。

## 未検証のもの

- **3本とも実際に公開されるかは未確認**（最初は 9/2 18:30）。
- **写真の見栄えに差がある。** 玉城氏は公式の笑顔ポートレート、古謝氏は
  伏し目のスナップ。**どちらも ja.wikipedia の記事画像**で、規約上ほかの
  取得経路は無い（Commons の人物名 全文検索は禁止）。差し替えるなら
  `fetch_photo.py` で手動置換 → 再ビルド → 再アップロードになる。**未対応。**
- **選挙運動にあたるかの判断は未整理。** 3本とも「候補者が公表している内容を
  そのまま紹介する」形にしてあり、賛否は書いていない。ただし
  公職選挙法142条の3第3項は、選挙運動用のウェブサイト等に**連絡先の表示**を
  求めている。中立な紹介が選挙運動にあたらないという整理は**法的助言ではない**。
  気になるなら説明欄に連絡先を足すのが安全側。
- **9/13（投票日当日）の新規投稿・更新は公職選挙法129条で禁止。**
  この3本は 9/2〜9/4 公開なので該当しないが、**9/13 に選挙関連の投稿をしない**こと。
- **本数は古謝3本・玉城2本。** 完全な対称ではないが、要約と深掘りは
  両候補ぶん揃えた。
- **写真の見え方が候補で違う。** 古謝氏は余白を足したぶん顔が小さくなり
  左右にぼかしが入る。玉城氏は元画像に余白があるので全面に大きく出る。
  素材の差が原因で、意図した演出ではない。気になるなら玉城氏側にも
  `frame_photo.py` をかけて揃えられるが、**そうすると玉城氏の顔も小さくなる**。
- **TikTok 版は作っていない。** `run_election.py` は TikTok を触らない。
- TikTok の審査状況は前回から未確認（前回は in review）。

## 次にやること

1. **9/2 18:30 に `vvpSRwF072M` が公開されたか見る。** 選挙題材の初公開。
   事故があれば `python scripts/unpublish.py vvpSRwF072M` で即座に戻せる。
2. **写真を差し替えるか決める**（上記「未検証のもの」）。差し替えるなら
   9/3・9/4 のぶんは間に合う。
3. **`com.-youtube` の Google OAuth トークンを失効・再発行する**（上記）。
   そのあとで履歴を掃除しないと、あのリポジトリは push できないままになる。

4. **不要なら旧版3本（`vvpSRwF072M` / `yHT9_jXuvlY` / `BgIhdx43dSo`）を
   YouTube Studio から削除する。** 予約は外してあるので放置しても公開されない。

5. **9/7 以降の枠は空。** 通常の題材に戻すなら従来どおり:

   ```bash
   python scripts/run_daily.py --keyword "<2語以上>" --script <台本.json> --days-ahead N --limit 1
   ```

   実測済みで未使用の検索語候補: `医師 偏在`、`外国人 土地`。
6. **選挙が終わったら（9/13以降）`scripts/election.py` と
   `scripts/run_election.py` を消す。** 消せば元の1経路に戻る。
   テスト `tests/test_election.py` も一緒に消す。
7. 維持率の確認（`python scripts/retention_report.py`）。選挙4本は
   通常の題材と傾向が違うはずなので、平常の題材選びの判断材料に混ぜないこと。

## 触ってはいけないところ

- **`evidence.EVIDENCE_HOST_SUFFIX` を広げない。** 今回はそこを触らずに
  別経路を作ることで対応した。広げると今後の全動画に効く。
- **`election.MANIFESTO_SOURCES` を候補ごとに不揃いにしない。**
  片方だけ資料が多い状態にすると、この経路が一方の候補の宣伝装置になる。
  候補ごとの件数が揃っていることをテストが縛っている。
- **`election.MIN_PDF_CHARS` / `MIN_KANA_RATIO` を下げない。** テキスト層の無い
  PDFや文字化けした抽出結果を通すと逐語照合の対象が消え、この経路唯一の関門が
  無効になる。玉城氏の政策集PDFで実際に踏んだ。
- **`build_short._fill` を「切り取らない」方式に変えない。** 余白のある素材の
  構図まで変わる。顔が切れるのは素材側の問題なので `frame_photo.py` で直す。
- **`draw._BACKTRACK_RATIO` を下げすぎない。** 戻せる幅を広げるほど自然な
  切れ目は見つかるが、1行だけ極端に短い段差ができて逆に読みにくくなる。
  0.6 は実測（158か所）で決めた値。
- **`election.collect` の逐語照合を緩めない。** これがこの経路唯一の関門。
- 台本ファイルの `source_url` 必須をやめない。`source_quote` も同様。
- **台本を書いたら `check_telop.py` で幅を測ってからビルドする。**
- `state/*.json` を手で編集しない。
- チャンネルを動かす操作の前に main を取り込んで state を最新にする。
- **9/13（投票日当日）に選挙関連の投稿・更新をしない**（公職選挙法129条）。
